version: '2'
services:
    ecolarmobile-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb ecolarmobile-mongodb-config
    ecolarmobile-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    ecolarmobile-mongodb-config:
        image: mongo:4.0.2
        container_name: ecolarmobile-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
